{
  "name": "mcp-insurance-project",
  "version": "1.0.0",
  "description": "MCP Insurance Server with Openkoda API integration",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "build": "cd backend && npm run build",
    "install-all": "npm install && cd backend && npm install"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.1",
    "axios": "^1.10.0",
    "dotenv": "^17.2.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.0.13",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
